import 'package:flutter/material.dart';

class NotificationReciver extends StatelessWidget {
  const NotificationReciver({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('notif'),
      ),
    );
  }
}
